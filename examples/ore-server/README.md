# Ore Server Example

This example demonstrates how to run a HyperStack server for the Ore protocol.

## Running

```bash
cargo run
```

The server will start on `[::]:8878` and stream Ore round entities.

## Stack

This server uses the ore-stack from `../../stacks/ore`.
