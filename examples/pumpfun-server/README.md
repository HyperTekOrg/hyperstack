# PumpFun Server Example

This example demonstrates how to run a HyperStack server for the PumpFun protocol.

## Running

```bash
cargo run
```

The server will start on `[::]:8877` and stream PumpFun token entities.

## Stack

This server uses the pumpfun-stack from `../../stacks/pumpfun`.
