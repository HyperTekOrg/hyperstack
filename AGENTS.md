# Repository guidance

Use the repository README and existing build/test commands. If
`.arete-agent/CONTEXT.md` exists, read it for optional local developer context.
Public use does not require the internal workspace or its tools.
